export const TERM_MAX_LENGTH = 35;
export const DEFINITION_MAX_LENGTH = 45;
export const IMPORT_MAX_ITEMS = 50;
export const VOCABULARY_MAX_ITEMS = 500;

export type VocabularyInput = {
  term: string;
  definition: string;
};

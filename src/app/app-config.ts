import type { AppId } from "@/app/_languages/registry";

export type { AppId } from "@/app/_languages/registry";

export const DEFAULT_APP_ID: AppId = "en";
export const APP_HEADER = "x-memento-app";
